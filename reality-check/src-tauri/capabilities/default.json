{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the Hima dashboard and prompt windows",
  "windows": ["main", "prompt"],
  "permissions": ["core:default"]
}
